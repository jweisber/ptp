bookdown::gitbook:
  css: custom.css
  includes:
    before_body: preamble.tex
  split_by: section
  config:
    toc:
      collapse: section
    fontsettings:
      theme: night
      family: serif
      size: 2
